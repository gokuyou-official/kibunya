{
  "name": "kibunya-rules-tests",
  "private": true,
  "type": "module",
  "description": "firestore.rules の回帰テスト。ルート package.json とは独立させ、EAS ビルドの依存に影響を与えない。",
  "scripts": {
    "test": "firebase emulators:exec --only firestore --project demo-kibunya \"node rules.test.mjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.1",
    "firebase": "^12.17.1",
    "firebase-tools": "^15.26.0"
  }
}
